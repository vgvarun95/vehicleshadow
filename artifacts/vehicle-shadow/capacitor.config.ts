import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vehicleshadow.app',
  appName: 'Vehicle Shadow',
  webDir: 'dist/public'
};

export default config;
